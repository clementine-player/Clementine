To compile Clementine, use the command below

cmake ./CMakeLists.txt

If an developer files are needed (like libspotify), they will be listed. To obtain them, install the needed packages using your package manager or search the Internet for the needed packages.
